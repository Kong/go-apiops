# kced temporary documentation

Since the `kced` command in intended to be temporary, so is its documentation...

## command `openapi2kong`

For details on the format see the [annotated example file](learnservice_oas.yml).
